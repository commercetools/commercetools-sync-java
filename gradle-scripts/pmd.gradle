/**
 * PMD tool
 */
pmd {
    toolVersion = pmdVersion
    ruleSetConfig = rootProject.resources.text.fromFile('config/pmd/pmd.xml')
    rulePriority = 1
    consoleOutput = true
}

tasks.withType(Pmd) {
    reports {
        html.enabled = false
        xml.enabled = false
    }
}
